<%= simple_form_for [@board, @category] do |f| %>
  <%= f.input :name, label: false, input_html: {placeholder: "Category Name"} %>
  <%= f.submit 'Submit'.html_safe, class: 'btn btn-lg btn-primary' %>
  <%= link_to 'Cancel', :back, class: 'btn btn-lg btn-default' %>
<% end %>
